# Auto-generated by build_pro_deck.py — do not edit by hand.
# Paste your Solution class where indicated, then run to self-test.

import random
import functools
import collections
import string
import math
import datetime

from typing import *
from functools import *
from collections import *
from itertools import *
from heapq import *
from bisect import *
from string import *
from operator import *
from math import *

inf = float('inf')

class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

def list_node(values: list):
    if not values:
        return None
    head = ListNode(values[0])
    p = head
    for val in values[1:]:
        node = ListNode(val)
        p.next = node
        p = node
    return head

def is_same_list(p1, p2):
    if p1 is None and p2 is None:
        return True
    if not p1 or not p2:
        return False
    return p1.val == p2.val and is_same_list(p1.next, p2.next)

class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

def tree_node(values: list):
    if not values:
        return None
    root = TreeNode(values[0])
    i = 1
    queue = deque()
    queue.append(root)
    while queue:
        node = queue.popleft()
        if i < len(values) and values[i] is not None:
            node.left = TreeNode(values[i])
            queue.append(node.left)
        i += 1
        if i < len(values) and values[i] is not None:
            node.right = TreeNode(values[i])
            queue.append(node.right)
        i += 1
    return root

def is_same_tree(p, q):
    if not p and not q:
        return True
    elif not p or not q:
        return False
    elif p.val != q.val:
        return False
    else:
        return is_same_tree(p.left, q.left) and is_same_tree(p.right, q.right)


# ==== YOUR SOLUTION HERE ====


# Test cases for happy-number
_test_lines = [
    'assert candidate(100) == True',
    'assert candidate(4) == False',
    'assert candidate(20) == False',
    'assert candidate(2) == False',
    'assert candidate(1111111) == True',
    'assert candidate(19) == True',
    'assert candidate(1) == True',
    'assert candidate(1000000000) == True',
    'assert candidate(7) == True',
    'assert candidate(489) == False',
    'assert candidate(789789789) == False',
    'assert candidate(130) == True',
    'assert candidate(49) == True',
    'assert candidate(2147483647) == False',
    'assert candidate(32442) == True',
    'assert candidate(324) == False',
    'assert candidate(203) == True',
    'assert candidate(82) == True',
    'assert candidate(70) == True',
    'assert candidate(2736895) == False',
    'assert candidate(1000) == True',
    'assert candidate(44) == True',
    'assert candidate(190) == True',
    'assert candidate(58) == False',
    'assert candidate(28) == True',
    'assert candidate(289) == False',
    'assert candidate(444444444) == False',
    'assert candidate(14659) == False',
    'assert candidate(6892) == False',
    'assert candidate(3249671058) == False',
    'assert candidate(145) == False',
    'assert candidate(14657) == False',
    'assert candidate(10000) == True',
    'assert candidate(23) == True',
    'assert candidate(784) == True',
    'assert candidate(133) == True',
    'assert candidate(999) == False',
    'assert candidate(89) == False',
    'assert candidate(9474) == False',
    'assert candidate(111111111) == False',
    'assert candidate(404) == True',
    'assert candidate(442) == False',
    'assert candidate(987654321) == False',
    'assert candidate(1234567890) == False',
    'assert candidate(139) == True',
    'assert candidate(1000000007) == False',
    'assert candidate(13441) == False',
    'assert candidate(200) == False',
    'assert candidate(8989898989) == False',
    'assert candidate(3249) == False',
    'assert candidate(123456789) == False',
    'assert candidate(370) == False',
    'assert candidate(789999) == False',
    'assert candidate(1111111111) == True',
    'assert candidate(100000000) == True',
]

def check(candidate):
    passed = 0
    failed = []
    ns = dict(globals())
    ns['candidate'] = candidate
    for i, line in enumerate(_test_lines, 1):
        try:
            exec(line, ns)
            passed += 1
        except AssertionError:
            failed.append((i, line, 'wrong answer'))
        except Exception as e:
            failed.append((i, line, type(e).__name__ + ': ' + str(e)))
    total = len(_test_lines)
    print(f'{passed}/{total} tests passed')
    if failed:
        print(f'\n{len(failed)} test(s) failed:')
        for i, line, err in failed[:10]:
            print(f'  Test #{i}: {err}')
            print(f'    {line}')
        if len(failed) > 10:
            print(f'  ... and {len(failed) - 10} more')
    else:
        print('All tests passed! \u2713')
    return not failed

check(Solution().isHappy)
