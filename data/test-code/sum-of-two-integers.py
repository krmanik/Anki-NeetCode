# Auto-generated by build_pro_deck.py — do not edit by hand.
# Paste your Solution class where indicated, then run to self-test.

import random
import functools
import collections
import string
import math
import datetime

from typing import *
from functools import *
from collections import *
from itertools import *
from heapq import *
from bisect import *
from string import *
from operator import *
from math import *

inf = float('inf')

class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

def list_node(values: list):
    if not values:
        return None
    head = ListNode(values[0])
    p = head
    for val in values[1:]:
        node = ListNode(val)
        p.next = node
        p = node
    return head

def is_same_list(p1, p2):
    if p1 is None and p2 is None:
        return True
    if not p1 or not p2:
        return False
    return p1.val == p2.val and is_same_list(p1.next, p2.next)

class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

def tree_node(values: list):
    if not values:
        return None
    root = TreeNode(values[0])
    i = 1
    queue = deque()
    queue.append(root)
    while queue:
        node = queue.popleft()
        if i < len(values) and values[i] is not None:
            node.left = TreeNode(values[i])
            queue.append(node.left)
        i += 1
        if i < len(values) and values[i] is not None:
            node.right = TreeNode(values[i])
            queue.append(node.right)
        i += 1
    return root

def is_same_tree(p, q):
    if not p and not q:
        return True
    elif not p or not q:
        return False
    elif p.val != q.val:
        return False
    else:
        return is_same_tree(p.left, q.left) and is_same_tree(p.right, q.right)


# ==== YOUR SOLUTION HERE ====


# Test cases for sum-of-two-integers
_test_lines = [
    'assert candidate(1000, -1000) == 0',
    'assert candidate(-2, -3) == -5',
    'assert candidate(0, 5) == 5',
    'assert candidate(500, 500) == 1000',
    'assert candidate(-5, -5) == -10',
    'assert candidate(1, 2) == 3',
    'assert candidate(999, 1) == 1000',
    'assert candidate(-5, 5) == 0',
    'assert candidate(-1000, 1000) == 0',
    'assert candidate(-500, 500) == 0',
    'assert candidate(-500, -500) == -1000',
    'assert candidate(0, 0) == 0',
    'assert candidate(-1, 1) == 0',
    'assert candidate(2, 3) == 5',
    'assert candidate(-999, -1) == -1000',
    'assert candidate(-7, -13) == -20',
    'assert candidate(-999, 1) == -998',
    'assert candidate(-128, 256) == 128',
    'assert candidate(-999, 999) == 0',
    'assert candidate(1234, -5678) == -4444',
    'assert candidate(7, 13) == 20',
    'assert candidate(7, -7) == 0',
    'assert candidate(256, 255) == 511',
    'assert candidate(0, -1) == -1',
    'assert candidate(-223, -777) == -1000',
    'assert candidate(-1000, -999) == -1999',
    'assert candidate(-1234, 5678) == 4444',
    'assert candidate(-123, 456) == 333',
    'assert candidate(1, 0) == 1',
    'assert candidate(7, -3) == 4',
    'assert candidate(678, 322) == 1000',
    'assert candidate(500, 501) == 1001',
    'assert candidate(-789, 321) == -468',
    'assert candidate(1, 999) == 1000',
    'assert candidate(456, 123) == 579',
    'assert candidate(1, -1) == 0',
    'assert candidate(7, -13) == -6',
    'assert candidate(512, -512) == 0',
    'assert candidate(-314, 157) == -157',
    'assert candidate(499, 500) == 999',
    'assert candidate(256, 256) == 512',
    'assert candidate(314, -157) == 157',
    'assert candidate(223, -777) == -554',
    'assert candidate(1234, -1234) == 0',
    'assert candidate(-1, -999) == -1000',
    'assert candidate(456, -789) == -333',
    'assert candidate(456, -234) == 222',
    'assert candidate(-777, -223) == -1000',
    'assert candidate(-678, -322) == -1000',
    'assert candidate(789, -321) == 468',
    'assert candidate(-333, -667) == -1000',
    'assert candidate(0, -1000) == -1000',
    'assert candidate(-7, 3) == -4',
    'assert candidate(-456, -123) == -579',
    'assert candidate(-500, 250) == -250',
    'assert candidate(345, 678) == 1023',
    'assert candidate(1000, 0) == 1000',
    'assert candidate(-321, 654) == 333',
    'assert candidate(333, 667) == 1000',
    'assert candidate(0, 1000) == 1000',
    'assert candidate(-256, -256) == -512',
    'assert candidate(-777, 223) == -554',
    'assert candidate(-1000, 0) == -1000',
    'assert candidate(-123, -456) == -579',
    'assert candidate(-456, 123) == -333',
    'assert candidate(777, 223) == 1000',
    'assert candidate(-789, 456) == -333',
    'assert candidate(63, 127) == 190',
    'assert candidate(-500, -499) == -999',
    'assert candidate(100, -100) == 0',
    'assert candidate(-1, 0) == -1',
    'assert candidate(999, -999) == 0',
    'assert candidate(1000, -999) == 1',
    'assert candidate(123, 456) == 579',
    'assert candidate(1000, 999) == 1999',
    'assert candidate(123, -456) == -333',
    'assert candidate(-256, -255) == -511',
    'assert candidate(500, -250) == 250',
    'assert candidate(1024, -1024) == 0',
    'assert candidate(-7, 13) == 6',
]

def check(candidate):
    passed = 0
    failed = []
    ns = dict(globals())
    ns['candidate'] = candidate
    for i, line in enumerate(_test_lines, 1):
        try:
            exec(line, ns)
            passed += 1
        except AssertionError:
            failed.append((i, line, 'wrong answer'))
        except Exception as e:
            failed.append((i, line, type(e).__name__ + ': ' + str(e)))
    total = len(_test_lines)
    print(f'{passed}/{total} tests passed')
    if failed:
        print(f'\n{len(failed)} test(s) failed:')
        for i, line, err in failed[:10]:
            print(f'  Test #{i}: {err}')
            print(f'    {line}')
        if len(failed) > 10:
            print(f'  ... and {len(failed) - 10} more')
    else:
        print('All tests passed! \u2713')
    return not failed

check(Solution().getSum)
