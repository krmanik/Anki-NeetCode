# Auto-generated by build_pro_deck.py — do not edit by hand.
# Paste your Solution class where indicated, then run to self-test.

import random
import functools
import collections
import string
import math
import datetime

from typing import *
from functools import *
from collections import *
from itertools import *
from heapq import *
from bisect import *
from string import *
from operator import *
from math import *

inf = float('inf')

class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

def list_node(values: list):
    if not values:
        return None
    head = ListNode(values[0])
    p = head
    for val in values[1:]:
        node = ListNode(val)
        p.next = node
        p = node
    return head

def is_same_list(p1, p2):
    if p1 is None and p2 is None:
        return True
    if not p1 or not p2:
        return False
    return p1.val == p2.val and is_same_list(p1.next, p2.next)

class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

def tree_node(values: list):
    if not values:
        return None
    root = TreeNode(values[0])
    i = 1
    queue = deque()
    queue.append(root)
    while queue:
        node = queue.popleft()
        if i < len(values) and values[i] is not None:
            node.left = TreeNode(values[i])
            queue.append(node.left)
        i += 1
        if i < len(values) and values[i] is not None:
            node.right = TreeNode(values[i])
            queue.append(node.right)
        i += 1
    return root

def is_same_tree(p, q):
    if not p and not q:
        return True
    elif not p or not q:
        return False
    elif p.val != q.val:
        return False
    else:
        return is_same_tree(p.left, q.left) and is_same_tree(p.right, q.right)


# ==== YOUR SOLUTION HERE ====


# Test cases for reverse-integer
_test_lines = [
    'assert candidate(-2147483412) == -2143847412',
    'assert candidate(2147483647) == 0',
    'assert candidate(120) == 21',
    'assert candidate(-123) == -321',
    'assert candidate(1534236469) == 0',
    'assert candidate(0) == 0',
    'assert candidate(-2147483648) == 0',
    'assert candidate(-1534236469) == 0',
    'assert candidate(123) == 321',
    'assert candidate(-10) == -1',
    'assert candidate(-100000) == -1',
    'assert candidate(10) == 1',
    'assert candidate(-999999999) == -999999999',
    'assert candidate(1) == 1',
    'assert candidate(2147483646) == 0',
    'assert candidate(-123000) == -321',
    'assert candidate(-900000) == -9',
    'assert candidate(-100100100) == -1001001',
    'assert candidate(-2147483647) == 0',
    'assert candidate(-1010101010) == -101010101',
    'assert candidate(1000000001) == 1000000001',
    'assert candidate(-1) == -1',
    'assert candidate(123000) == 321',
    'assert candidate(-2000000002) == -2000000002',
    'assert candidate(101010101) == 101010101',
    'assert candidate(1111111111) == 1111111111',
    'assert candidate(2147447412) == 2147447412',
    'assert candidate(-101010101) == -101010101',
    'assert candidate(900000) == 9',
    'assert candidate(987654321) == 123456789',
    'assert candidate(999999999) == 999999999',
    'assert candidate(-1000000000) == -1',
    'assert candidate(1001001001) == 1001001001',
    'assert candidate(-987654321) == -123456789',
    'assert candidate(-1000000001) == -1000000001',
    'assert candidate(10000000000) == 1',
    'assert candidate(11000000001) == 0',
    'assert candidate(876543210) == 12345678',
    'assert candidate(2147483640) == 463847412',
    'assert candidate(100100100) == 1001001',
    'assert candidate(100000) == 1',
    'assert candidate(2000000002) == 2000000002',
    'assert candidate(-9646324351) == -1534236469',
    'assert candidate(-123456789) == -987654321',
    'assert candidate(7463847412) == 2147483647',
    'assert candidate(-1000000003) == 0',
    'assert candidate(-3000000001) == -1000000003',
    'assert candidate(-876543210) == -12345678',
    'assert candidate(-1111111111) == -1111111111',
    'assert candidate(-1001001001) == -1001001001',
    'assert candidate(9646324351) == 1534236469',
    'assert candidate(1010101010) == 101010101',
    'assert candidate(123456789) == 987654321',
    'assert candidate(-10000000000) == -1',
    'assert candidate(-2147483640) == -463847412',
    'assert candidate(-7463847412) == -2147483647',
    'assert candidate(3000000001) == 1000000003',
    'assert candidate(1000000000) == 1',
    'assert candidate(1000000003) == 0',
    'assert candidate(-214748364) == -463847412',
    'assert candidate(9000000000) == 9',
]

def check(candidate):
    passed = 0
    failed = []
    ns = dict(globals())
    ns['candidate'] = candidate
    for i, line in enumerate(_test_lines, 1):
        try:
            exec(line, ns)
            passed += 1
        except AssertionError:
            failed.append((i, line, 'wrong answer'))
        except Exception as e:
            failed.append((i, line, type(e).__name__ + ': ' + str(e)))
    total = len(_test_lines)
    print(f'{passed}/{total} tests passed')
    if failed:
        print(f'\n{len(failed)} test(s) failed:')
        for i, line, err in failed[:10]:
            print(f'  Test #{i}: {err}')
            print(f'    {line}')
        if len(failed) > 10:
            print(f'  ... and {len(failed) - 10} more')
    else:
        print('All tests passed! \u2713')
    return not failed

check(Solution().reverse)
